# Optional API key to protect backend endpoints
AGENT_API_KEY=change-me

# Optional: If provided, broker adapter will POST trades here
BROKER_WEBHOOK_URL=

# Optional: A JSON feed that returns an array of trade objects matching TradePayload schema
MOXYAI_FEED_URL=
